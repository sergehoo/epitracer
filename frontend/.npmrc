# Plusieurs libs (react-leaflet, react-signature-canvas, etc.) ont
# des peerDependencies trop strictes. Cette option laisse npm résoudre
# malgré les conflits (équivalent du comportement pre-npm 7).
legacy-peer-deps=true
fund=false
audit=false
